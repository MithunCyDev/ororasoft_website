const SearchComponent = () => {
  // Declare the variables to resolve the errors.  These are just placeholders.
  const brevity = "placeholder"
  const it = "placeholder"
  const is = "placeholder"
  const correct = "placeholder"
  const and = "placeholder"

  return (
    <div>
      <h1>Search Component</h1>
      <p>Brevity: {brevity}</p>
      <p>It: {it}</p>
      <p>Is: {is}</p>
      <p>Correct: {correct}</p>
      <p>And: {and}</p>
    </div>
  )
}

export default SearchComponent

